# 🚀 发布新版本 - 快速命令参考

## 📝 完整发布流程

### 1️⃣ 修改版本号

编辑 `package.json`，更新版本号：

```json
{
  "version": "1.0.1"  // 从 1.0.0 改为 1.0.1
}
```

### 2️⃣ 提交所有更改

```bash
git add .
git commit -m "chore: release v1.0.1"
git push
```

### 3️⃣ 创建并推送标签（触发自动构建）

```bash
git tag v1.0.1
git push origin v1.0.1
```

### 4️⃣ 等待构建完成

- 访问：https://github.com/你的用户名/kitty-reminder/actions
- 等待 10-15 分钟
- 完成后访问：https://github.com/你的用户名/kitty-reminder/releases

---

## ⚡ 一键复制命令

### 发布 v1.0.1

```bash
# 1. 先手动修改 package.json 中的版本号为 1.0.1

# 2. 然后执行以下命令
git add .
git commit -m "chore: release v1.0.1"
git push
git tag v1.0.1
git push origin v1.0.1
```

### 发布 v1.0.2

```bash
# 1. 先手动修改 package.json 中的版本号为 1.0.2

# 2. 然后执行以下命令
git add .
git commit -m "chore: release v1.0.2"
git push
git tag v1.0.2
git push origin v1.0.2
```

### 发布 v1.1.0

```bash
# 1. 先手动修改 package.json 中的版本号为 1.1.0

# 2. 然后执行以下命令
git add .
git commit -m "feat: release v1.1.0"
git push
git tag v1.1.0
git push origin v1.1.0
```

---

## 🔄 如果发布错误需要重新发布

### 删除错误的标签

```bash
# 删除本地标签
git tag -d v1.0.1

# 删除远程标签
git push origin :refs/tags/v1.0.1
```

### 重新创建标签

```bash
# 修复代码后提交
git add .
git commit -m "fix: 修复问题"
git push

# 重新创建标签
git tag v1.0.1
git push origin v1.0.1
```

---

## 📋 版本号规范

遵循 [语义化版本](https://semver.org/lang/zh-CN/)：

| 版本类型 | 格式 | 说明 | 示例 |
|---------|------|------|------|
| **修订版本** | `v1.0.X` | Bug 修复、小改进 | v1.0.1, v1.0.2 |
| **次版本** | `v1.X.0` | 新功能、向下兼容 | v1.1.0, v1.2.0 |
| **主版本** | `vX.0.0` | 重大更新、不兼容 | v2.0.0, v3.0.0 |

### 示例：

- `v1.0.0` → `v1.0.1` - 修复了一个 bug
- `v1.0.1` → `v1.1.0` - 添加了新功能
- `v1.1.0` → `v2.0.0` - 重大更新，可能不兼容旧版本

---

## ✅ 发布前检查清单

- [ ] 本地测试通过（`npm start`）
- [ ] 所有更改已提交
- [ ] 更新了 `package.json` 中的版本号
- [ ] 版本号格式正确（v1.0.1）
- [ ] 准备好更新日志内容

---

## 🎯 常用场景

### 场景 1：修复了一个 Bug

```bash
# 1. 修改 package.json: "version": "1.0.1"
# 2. 执行命令
git add .
git commit -m "fix: 修复提醒重复触发的问题"
git push
git tag v1.0.1
git push origin v1.0.1
```

### 场景 2：添加了新功能

```bash
# 1. 修改 package.json: "version": "1.1.0"
# 2. 执行命令
git add .
git commit -m "feat: 添加深色模式支持"
git push
git tag v1.1.0
git push origin v1.1.0
```

### 场景 3：重大更新

```bash
# 1. 修改 package.json: "version": "2.0.0"
# 2. 执行命令
git add .
git commit -m "feat!: 重构提醒系统"
git push
git tag v2.0.0
git push origin v2.0.0
```

---

## 🔍 查看当前版本

```bash
# 查看 package.json 中的版本
cat package.json | grep version

# 查看所有标签
git tag

# 查看最新标签
git describe --tags --abbrev=0
```

---

## 💡 提示

1. **标签必须以 `v` 开头**，否则不会触发构建
2. **版本号要和 package.json 一致**
3. **推送标签才会触发构建**，普通 push 不会
4. **构建需要 10-15 分钟**，耐心等待
5. **可以在 Actions 页面查看构建进度**

---

## 🆘 遇到问题？

### 构建失败
- 查看 Actions 日志：https://github.com/你的用户名/kitty-reminder/actions
- 检查错误信息
- 参考 [CI-CD.md](docs/CI-CD.md) 的故障排除部分

### 标签推送失败
```bash
# 确认标签是否存在
git tag

# 确认远程仓库
git remote -v

# 重新推送
git push origin v1.0.1 --force
```

### 忘记修改版本号
```bash
# 删除标签
git tag -d v1.0.1
git push origin :refs/tags/v1.0.1

# 修改 package.json
# 重新提交和打标签
git add package.json
git commit -m "chore: update version to 1.0.1"
git push
git tag v1.0.1
git push origin v1.0.1
```
